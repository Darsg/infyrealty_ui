export interface UserForm {
    id?: number;
    name: string;
    contact_code: string;
    contact_no: string;
    email: string;
    address: string;
    description: string;
    password?: string;
}